{
  "name": "revo",
  "version": "1.0.0",
  "description": "",

  "scripts": {
    "clean-dist" :"rm -rf dist && rm -rf .parcel-cache && mkdir dist",
    "copy-img" :"cp -vR ./images ./dist",
    "build": "npm run clean-dist && npm run copy-img && parcel build index.html"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "http-server": "^14.1.1",
    "three": "^0.157.0"
  },
  "devDependencies": {
    "parcel": "^2.9.3"
  }
}
